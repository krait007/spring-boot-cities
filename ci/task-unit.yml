---
platform: linux

image_resource:
  type: docker-image
  source: {repository: "docker.pivotalpws.cn:5000/java"}  

inputs:
- name: cities-repo

run:
  path: bash
  args: 
  - -c
  - cd cities-repo/cities-service && ../gradlew test